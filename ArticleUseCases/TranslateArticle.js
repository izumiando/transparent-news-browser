// TranslateArticle Use Case
// question, should one TranslateArticle object  be initialized for every article or can one object be used for multiple objects?

class TranslateArticle {
    constructor(){
        // nothing to init
    }

    execute(article, language, translator){
        // take the html file from the article 
        // translate it to the language using the specified translator
        return translatedHTML;
    }
}